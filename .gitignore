# Repository-wide (project-specific rules live in backend/, frontend/, sdk/, samples/)

# Secrets
.env
.env.*
!.env.example

# Local NuGet feed (maintainer packs SDK for sample restore before publish)
.local-nuget/

# OS
.DS_Store
.AppleDouble
._*
Thumbs.db
ehthumbs.db
Desktop.ini

# IDE / editor
.idea/
*.code-workspace
*.swp
*~

# Local tooling (not part of the product)
agent-transcripts/

# Common build artifacts (nested .gitignore files are authoritative per project)
artifacts/
node_modules/
[Bb]in/
[Oo]bj/
dist/
.vs/

# Custom files
Prompt Mestre de Engenharia_ Reconstrução do Kyvo IDP (Foco Exclusivo em Identidade).md
prompt_mestre_integracao_dominio_kyvo_idp