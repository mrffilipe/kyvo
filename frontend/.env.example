# All variables below are optional in local development; the values shown here are also the
# built-in defaults loaded by frontend/src/config/env.ts when the variable is missing or empty.

VITE_API_BASE_URL=http://localhost:5000
VITE_API_VERSION=1.0
VITE_API_TIMEOUT_MS=30000
VITE_OAUTH_CLIENT_ID=platform-admin-web
VITE_OAUTH_REDIRECT_URI=http://localhost:3000/auth/callback
